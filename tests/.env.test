# Test environment configuration for ASFConnector
# This file is used in CI/CD pipelines and local testing

# ASF IPC Connection Settings (Mock values for testing)
ASF_HOST=127.0.0.1
ASF_PORT=1242
ASF_PASSWORD=test_password_for_ci
ASF_PATH=/Api

# Testing configuration
ENVIRONMENT=test
ENABLE_RICH_TRACEBACK=false

# Pytest configuration
PYTEST_TIMEOUT=30