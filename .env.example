# ASF IPC Connection Settings
# Copy this file to .env and update with your actual values

# ASF IPC Host (default: 127.0.0.1)
ASF_HOST=127.0.0.1

# ASF IPC Port (default: 1242)
ASF_PORT=1242

# ASF IPC Password (leave empty if no password is set)
ASF_PASSWORD=

# ASF IPC API Path (default: /Api)
ASF_PATH=/Api